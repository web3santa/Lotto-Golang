<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{.title}}</title>
  </head>
  <body>
    <h1>Hello Lotto</h1>
    <form action="/" method="post">
      <label>How many ticket buy</label>
      <input type="number" name="ticket" />
      <button type="submit">Submit</button>
    </form>
    <p>Spend: ${{.spend}}</p>
    <p>Prize: ${{.prize}}</p>
    <p>Profit: ${{.profit}}</p>
  </body>
</html>
